---
import { getCollection } from "astro:content";

const videos = await getCollection("videos");
---

<div class="container mx-auto px-4 py-8">
  <h2 class="sm:text-3xl 1 md:text-4xl my-10">
    Las voces de periodistas argentinas
  </h2>
  <h3>
    Sus experiencias ayudan a conocer el estado de situación de las mujeres
    periodistas en Argentina.
  </h3>
  <div class="flex mt-6 px-16">
    {
      videos.map((video) => {
        const { slug, data, body } = video;
        const { name, videoUrl, poster } = data;
        return (
          <div class="w-1/3">
            <div>
              <div class="flex justify-center mb-4">
                <video
                  src={videoUrl}
                  controls
                  controlslist="nodownload"
                  poster={poster}
                  class="w-3/4"
                >
                  Tu navegador no admite el elemento <code>video</code>.
                </video>
              </div>
              <div class="text-center has-text-black w-3/4 mx-auto">
                <b>{name} </b> <br />
                {body}
              </div>
            </div>
          </div>
        );
      })
    }
  </div>
</div>

<style></style>
